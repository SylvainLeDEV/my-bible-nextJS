// If is in development, we use the development configuration
export const path = "http://localhost:3000/";

// If is in production, we use the production configuration
// export const path = "https://sylvain-blog.com";
